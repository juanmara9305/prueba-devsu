services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=accountdb'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
